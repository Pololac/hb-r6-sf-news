{% extends 'base.html.twig' %}

{% block body %}

  <h1 class="text-4xl font-bold text-center dark:text-mak">Newsletter</h1>

  <div class="w-full max-w-80">{{ form(newsletterForm) }}</div>

{% endblock %}